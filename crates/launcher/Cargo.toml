[package]
name = "zotero-bridge-launcher"
description = "Cross-platform zotero:// URI launcher"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
zotero-bridge-core = { path = "../core" }
open.workspace = true
thiserror.workspace = true
