# Zotero Bridge 便携配置
# 本文件与 exe 放在同一目录时即启用便携模式：
# 索引数据库默认保存在 exe 旁的 data\index.sqlite。
# 如需自定义数据库位置，取消下面一行的注释并修改（支持 %VAR% 和 ~）：

# [storage]
# database = "D:\\zotero-bridge\\index.sqlite"

# 链接文件内写入的 URI 格式。默认值等同当前 Zotero 定位链接：
# [mirror.windows]
# uri_template = "{select_uri}"
